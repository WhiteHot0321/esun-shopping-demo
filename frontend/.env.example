# Copy this file to .env and adjust for your local setup.
# Vite reads .env from the frontend/ directory. .env is git-ignored.

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080/api
