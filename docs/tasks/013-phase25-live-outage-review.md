# Phase 2.5 live outage test independent review

Date: 2026-09-16. Reviewer: Claude Code, read-only. No tests executed by reviewer.

**PASS**

Verified against `StockCacheService`, `OrderService`, `OrderTransactionService`, `OrderController`, `ApiResponse`, and `AbstractMySqlIntegrationTest` (read only to resolve ambiguity, as permitted):

- **Real MySQL/Redis, disposable Redis ownership**: `REDIS` is a `static final GenericContainer` declared only in this leaf class (not on the shared `AbstractMySqlIntegrationTest.MYSQL` singleton pattern), so pausing it (line 79) cannot affect any other concurrently-running test class. Paused state is verified via `inspectContainerCmd` (line 81-82) before load starts.
- **20 concurrent requests succeed while paused, with exact counts**: assertions at lines 96-98 execute inside the `try` block, before `unpauseContainerCmd` in the `finally` (line 100) — so they genuinely run while Redis is still paused. Arithmetic checks out: 1 warmup + 20 concurrent = 21 orders, stock 100→79, `order_detail` sum 21. `StockCacheService.tryDecrease` (source) times out fast (200ms configured) and latches `degraded=true`, causing `OrderTransactionService.doCreateOrder` to fall back to DB-authoritative locking/decrement — explaining why all 20 succeed without overselling.
- **Replay after unpause returns identical response, no double-deduction**: `doCreateOrder`'s duplicate-`requestId` path (`OrderTransactionService.java:58-65`) returns the original order before calling `reserveStock.run()`, so no cache/DB mutation occurs on replay. `ApiResponse.ok`/ApiResponse have no timestamp/trace fields, so byte-for-byte equality (line 110) is a meaningful check, not trivially true.
- **Latch bypasses healthy Redis even after DB reconciliation, incl. zero-stock**: `degraded` is a plain instance `AtomicBoolean` with no reset method except constructing a new `StockCacheService`. The test's `reconcile()` only rewrites raw Redis values via `StringRedisTemplate`; it can't touch the bean's `degraded` flag. So after reconcile (line 118, confirming `zero`→"0" and `audit()` empty), the next order (line 121) is still routed DB-only (redis key frozen at "78" while DB moves to 77), producing the drift entry asserted at line 124 — this is the actual mechanism proving the claim, not an assumption.
- **Fresh cache instance + real transactional proxy resumes cache reservations**: the second `reconcile()` (line 125) resyncs Redis to the true DB value (77) before `restartedCache` is built, so the subsequent real `tryDecrease` call correctly decrements Redis 77→76 in lockstep with the DB decrement, yielding `audit().isEmpty()` (line 133) — genuine resumption, not a bypass. `restartedOrders` uses the real `transactions` bean (Spring-managed `@Transactional` proxy), matching the "existing real DB transactional proxy" requirement (note: `restartedOrders` itself isn't Spring-proxied, so its own `@Retryable` doesn't apply, but the test doesn't rely on retry here).
- **Cleanup**: `unpauseContainerCmd` and `pool.shutdownNow()`/`awaitTermination` are in a `finally` guarding the concurrent phase; all futures are already resolved before shutdown, so no risk of interrupting in-flight work. No other test-owned resources are mutated.

No reproducible blocker found; counts, ordering, and the latch/reconciliation mechanics are all faithfully exercised by real production code paths rather than assumed.
