Goal:      Phase 3.1 B1 application and B2 backend are implemented, targeted-tested and independently reviewed PASS.
Changed:   Tasks 017–020 plus payment backend/frontend and product ownership/soft-delete/restock backend remain uncommitted in `codex/phase31-pay-status`.
Validated: Task 020 focused Maven tests 17/17 PASS after one repair; independent authorization/database read-only review PASS; Git status unchanged by reviewer. Local project-state and four required Notion pages were updated and read back.
Remaining: Implement B2 frontend ProductManagement (own-product list, active/deleted filter if API supports it, add/edit/delete confirmation, restock, immediate validation); then Phase 3.1-C full regression, populated-DB migration check, Phase 3.0 order-query prerequisite assessment, and live ECPay sandbox if credentials/callback reachability are available.
Risks:     Existing populated MySQL has not run `06_product_ownership.sql`; no Task 020 full suite yet; ECPay live sandbox requires external credentials and a reachable callback; current backend owner list returns active products only, so deleted-filter UX may require a bounded API adjustment.
Next:      Create a frontend-only Task 021 gate, inspect the current ShopWorkspace/API conventions, implement ProductManagement with focused Vitest coverage and production build, then independently review the ownership-sensitive UI/API integration.
Constraints: Preserve the dirty user checkout; work only in `C:\GitHub\esun-shopping-phase31`; no commit/push/merge; one writer; do not mark Phase 3.1 complete before C evidence.
