package com.esun.shop.model;

/** PERCENT: discountValue is a whole-number percentage off (1-99). FIXED: discountValue is a whole-dollar amount off. */
public enum DiscountType {
    PERCENT,
    FIXED
}
