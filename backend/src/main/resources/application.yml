server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:esun_shop}?useSSL=false&serverTimezone=Asia/Taipei&characterEncoding=utf8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    time-zone: Asia/Taipei

llm:
  provider: ${LLM_PROVIDER:ollama}
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
    chat-model: ${OLLAMA_CHAT_MODEL:llama3.1}
    embed-model: ${OLLAMA_EMBED_MODEL:nomic-embed-text}
    timeout-seconds: ${OLLAMA_TIMEOUT_SECONDS:30}
  claude:
    api-key: ${ANTHROPIC_API_KEY:}
    chat-model: claude-sonnet-5
  rag:
    top-k: ${RAG_TOP_K:4}
